'use strict';
var _ = require("lodash");
function calculate_elements_sum(collection, element) {
  //在这里写入代码
  var result = 
  _.indexOf(collection,element);
  return result;
}

module.exports = calculate_elements_sum;

